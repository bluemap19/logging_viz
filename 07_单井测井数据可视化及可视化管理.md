# 📈 单井测井数据可视化及可视化管理模块

> Single Well Logging Data Visualization Module

**版本：** v1.0  
**最后更新：** 2026-03-14  
**关联源文件：** `src_plot/data_visulization.py`, `src_plot/data_manager.py`, `src_plot/cache_logging.py`

---

## 一、模块概述

### 1.1 模块定位

单井测井数据可视化及可视化管理模块是整个测井数据可视化系统的**核心展示层**，负责将单井的多类型测井数据（常规测井、FMI 电成像、NMR 核磁、表格分类数据）以专业、交互式的方式呈现。该模块是用户与测井数据直接交互的窗口，提供深度导航、多道对比、岩性展示等核心可视化功能。

### 1.2 核心功能

| 功能 | 说明 |
|------|------|
| **多类型数据渲染** | 常规测井曲线、FMI 图像、NMR 谱图、岩性分类填充 |
| **交互式深度导航** | 滑动条控制窗口大小，鼠标滚轮滚动深度 |
| **自适应布局** | 根据数据类型自动调整子图数量和排列 |
| **高性能缓存** | 深度窗口数据缓存，支持快速滚动刷新 |
| **配置智能补全** | 自动检测数据并生成合理绘图配置 |
| **性能监控** | 实时统计渲染性能和缓存命中率 |

### 1.3 技术特点

- 基于 matplotlib 的专业测井可视化
- 支持 4 种数据类型同时显示
- 共享深度轴的多道并排布局
- 对象池优化 NMR 谱图渲染
- 压缩缓存减少内存占用
- 批量渲染提高分类数据显示性能

### 1.4 系统架构位置

```
┌─────────────────────────────────────────┐
│      应用层 (用户交互/导出/分析)          │
└─────────────────┬───────────────────────┘
                  │ 调用
                  ↓
┌─────────────────────────────────────────┐
│   可视化层 ← 本模块                       │
│  ┌──────────────────────────────────┐   │
│  │   WellLogVisualizer (可视化主类)  │   │
│  │   - 图形布局管理                  │   │
│  │   - 交互式控件                    │   │
│  │   - 多类型数据渲染                │   │
│  └──────────────────────────────────┘   │
│  ┌──────────────────────────────────┐   │
│  │ LoggingDataManager (数据管理器)   │   │
│  │   - 数据预处理                    │   │
│  │   - 配置验证                      │   │
│  │   - 缓存管理                      │   │
│  └──────────────────────────────────┘   │
│  ┌──────────────────────────────────┐   │
│  │  EnhancedWellLogCache (缓存系统)  │   │
│  │   - 深度窗口缓存                  │   │
│  │   - 数据压缩存储                  │   │
│  │   - 性能统计                      │   │
│  └──────────────────────────────────┘   │
└─────────────────┬───────────────────────┘
                  │ 读取
                  ↓
┌─────────────────────────────────────────┐
│      数据层 (DATA_WELL)                  │
│  - 常规测井 DataLogging                 │
│  - FMI 数据 DataFMI                      │
│  - NMR 数据 DataNMR                      │
│  - 表格数据 DataTable                   │
└─────────────────────────────────────────┘
```

---

## 二、目录结构与文件组织

### 2.1 源文件组织

```
src_plot/
├── data_visulization.py         # WellLogVisualizer 可视化主类
├── data_manager.py              # LoggingDataManager 数据管理器
├── cache_logging.py             # EnhancedWellLogCache 缓存系统
├── TEMP_TEST_VIZ.py             # 可视化测试脚本
└── __init__.py                  # 模块初始化

src_logging/
├── curve_preprocess.py          # 曲线预处理（深度分辨率计算）
├── logging_combine.py           # 测井数据合并
├── logging_interpolation.py     # 测井数据插值
└── __init__.py
```

### 2.2 模块依赖关系

```
data_visulization.py
    ↓ 导入
data_manager.py
    ↓ 导入
cache_logging.py
    ↓ 导入
curve_preprocess.py (get_resolution_by_depth)
```

---

## 三、WellLogVisualizer 可视化主类

### 3.1 类结构概览

```python
class WellLogVisualizer:
    """测井数据可视化类 - 专业级可视化工具"""
    
    # 核心功能：
    # - 多类型数据显示：常规测井曲线、岩性分类数据、FMI 图像数据、NMR 谱图
    # - 交互式导航：滑动条控制窗口大小，鼠标滚轮滚动深度
    # - 自适应布局：根据数据类型自动调整面板布局
```

### 3.2 核心配置常量

#### 颜色配置

```python
# 岩性分类颜色（5 种标准岩性）
DEFAULT_TYPE_COLORS = ['#00FFFF', '#00FF00', '#FFFF00', '#FF4500', '#000000']

# 测井曲线颜色（10 条曲线，深色系避免视觉疲劳）
DEFAULT_CURVE_COLORS = [
    '#FF0000',  # 红色
    '#006400',  # 深绿色
    '#00008B',  # 深蓝色
    '#00CED1',  # 深青色
    '#8B008B',  # 深洋红
    '#4B0082',  # 靛蓝色
    '#006D5B',  # 深青绿
    '#8B0000',  # 深红色
    '#FF8C00',  # 深橙色
    '#B8860B'   # 暗金色
]

# 多曲线道颜色序列（同一道内多条曲线）
DEFAULT_MULTI_CURVE_COLORS = ['#FF0000', '#000000', '#0000FF', '#800080', '#FFA500', '#FFFF00']
```

#### 布局配置

```python
LAYOUT_CONFIG = {
    # 边距配置（占图形宽度的比例）
    'left_margin': 0.06,           # 左侧边距
    'right_margin': 0.98,          # 右侧边距（为滑动条留空间）
    'legend_bottom_margin': 0.067, # 有图例时的底部边距
    'no_legend_bottom_margin': 0.062,  # 无图例时的底部边距
    
    # 标题配置
    'title_margin': 0.001,         # 标题框与子图的间距
    'title_height': 0.04,          # 标题框（数据头）高度
    'title_fontsize': 10,          # 标题字体大小
    
    # 滑动条配置
    'slider_width': 0.02,          # 滑动条宽度
    'min_window_size_ratio': 0.01, # 最小窗口大小比例
    'scroll_step_ratio': 0.1,      # 滚轮滚动步长比例
    
    # 字体配置
    'axis_label_fontsize': 10,     # 坐标轴标签字体
    'tick_label_fontsize': 8,      # 刻度标签字体
    'legend_fontsize': 10,         # 图例字体大小
    
    # 图例配置
    'legend_box_alpha': 0.8,       # 图例框透明度
    'legend_facecolor': '#FFFFFF', # 图例框颜色
    
    # 深度指示器配置
    'depth_indicator_alpha': 0.2,      # 深度指示器背景透明度
    'depth_indicator_facecolor': '#666666',  # 深度指示器背景颜色
    'depth_indicator_fontsize': 10,    # 深度指示器字体大小
}
```

#### 渲染配置

```python
RENDERING_CONFIG = {
    'curve_linewidth': 1.0,        # 曲线线宽
    'title_box_linewidth': 1,      # 标题框边框宽度
    'head_box_facecolor': '#f5f5f5',  # 道头背景颜色（浅灰色）
    'head_box_edgecolor': '#aaaaaa',  # 道头边框颜色（灰色）
}
```

#### NMR 配置

```python
NMR_CONFIG = {
    'X_LOG': [True, True],              # 是否使用对数坐标轴
    'NMR_TITLE': ['N1', 'N2'],          # NMR 面板标题
    'X_LIMIT': [[0.1, 1000], [0.1, 1000]],  # X 轴显示范围
    'Y_scaling_factor': 1.0,            # Y 轴缩放因子
    'JUMP_POINT': 50,                   # 采样跳跃点数
    'line_width': 0.8,                  # 谱线宽度
    'line_alpha': 0.9,                  # 谱线透明度
    'fill_alpha': 0.5,                  # 填充透明度
    'default_color': 'green',           # 默认颜色
    'fill_line_width': 0.8,             # 网格线透明度
    'amplitude_scale_base': 1.5,        # 振幅缩放基准值
    'amplitude_power_factor': 0.6       # 振幅幂次因子
}
```

#### 字体配置

```python
FONT_CONFIG = {
    'family': ['DejaVu Sans', 'SimHei', 'sans-serif'],  # 字体族（支持中文）
    'weight_bold': 'bold',  # 粗体字重
}
```

### 3.3 核心属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `data_manager` | LoggingDataManager | 数据管理器实例 |
| `config_logging` | Dict | 常规测井绘图配置 |
| `config_fmi` | Dict | FMI 电成像绘图配置 |
| `config_nmr` | Dict | NMR 核磁绘图配置 |
| `config_type` | Dict | 分类结果绘图配置 |
| `depth_min` | float | 数据最小深度 |
| `depth_max` | float | 数据最大深度 |
| `depth_position` | float | 当前视图顶部深度位置 |
| `window_size` | float | 当前显示窗口的深度范围 |
| `resolution` | float | 深度采样分辨率（点间距） |
| `fig` | plt.Figure | 主图形对象 |
| `axs` | List[plt.Axes] | 子图轴对象列表 |
| `window_size_slider` | Slider | 窗口大小滑动条 |
| `plots` | List | 曲线绘图对象列表 |
| `fmi_images` | List | FMI 图像对象列表 |
| `nmr_plots` | List | NMR 谱图绘图对象 |
| `class_axes` | List | 分类数据子图轴列表 |

### 3.4 核心方法详解

#### __init__(data_manager, config_logging, config_fmi, config_nmr, config_type)

**目的：** 初始化测井数据可视化器

**参数：**
- `data_manager`: LoggingDataManager 实例
- `config_logging`: 常规测井绘图配置字典
- `config_fmi`: 电成像绘图配置字典
- `config_nmr`: 核磁绘图配置字典
- `config_type`: 分类结果绘图配置字典

**初始化流程：**
1. 使用数据管理器验证各配置项
2. 存储配置和数据管理器引用
3. 初始化显示状态属性（深度范围、窗口大小等）
4. 初始化图形对象属性（fig、axs、slider 等）
5. 设置 matplotlib 字体（支持中文显示）

**代码示例：**
```python
def __init__(self, data_manager: LoggingDataManager=None,
             config_logging={}, config_fmi: Dict[str, Any]={},
             config_nmr: Dict[str, Any]={}, config_type: Dict[str, Any]={}) -> None:
    # 验证配置
    validated_configs = data_manager.plot_config_check(
        config_logging=config_logging, config_fmi=config_fmi,
        config_nmr=config_nmr, config_type=config_type
    )
    
    self.data_manager = data_manager
    self.config_logging = validated_configs['logging']
    self.config_fmi = validated_configs['fmi']
    self.config_nmr = validated_configs['nmr']
    self.config_type = validated_configs['type']
    
    # 初始化深度范围
    self.depth_min, self.depth_max = data_manager._get_depth_limits()
    self.depth_position = self.depth_min
    self.window_size = 0.1 * (self.depth_max - self.depth_min)
    
    # 设置字体
    self._setup_matplotlib_fonts()
```

#### _setup_matplotlib_fonts()

**目的：** 设置 matplotlib 字体以支持中文显示

**原理：**
1. 配置字体族（优先使用 DejaVu Sans，回退到 SimHei）
2. 禁用 Unicode 负号处理（避免中文环境下的显示问题）

**代码示例：**
```python
def _setup_matplotlib_fonts(self):
    """设置 matplotlib 字体 - 使用集中化的字体配置"""
    try:
        plt.rcParams['font.family'] = self.FONT_CONFIG['family']
        plt.rcParams['axes.unicode_minus'] = False
        self.logger.info("字体设置完成，使用配置：%s", self.FONT_CONFIG['family'])
    except Exception as e:
        self.logger.warning("字体设置失败：%s, 使用默认设置", e)
```

#### _calculate_subplot_count()

**目的：** 计算子图总数

**原理：**
1. 调用数据管理器的 `cal_plot_num()` 方法
2. 返回 FMI 面板数 + 曲线面板数 + 分类面板数 + NMR 谱面板数

**公式：**
```
num_total = n_curve_panels + n_type_panels + n_fmi_panels + n_nmr_panels
```

**代码示例：**
```python
def _calculate_subplot_count(self) -> int:
    """计算子图总数：FMI 面板 + 曲线面板 + 分类面板 + NMR 谱面板"""
    self.n_curve_panels, self.n_type_panels, self.n_fmi_panels, self.n_nmr_panels = \
        self.data_manager.cal_plot_num()
    
    self.num_total = self.n_curve_panels + self.n_type_panels + \
                     self.n_fmi_panels + self.n_nmr_panels
    
    if self.num_total == 0:
        self.logger.warning("没有可显示的子图内容，请至少提供一种数据类型")
    
    return self.num_total
```

#### _setup_figure_layout(figure, n_plots, has_legend, figsize)

**目的：** 设置图形布局和子图排列

**布局逻辑：**
1. 创建或重用图形对象
2. 创建 1 行 n_plots 列的子图，共享 Y 轴（深度轴）
3. 调整边距为图例和滑动条留空间
4. 子图间无间距（wspace=0）确保紧凑布局

**代码示例：**
```python
def _setup_figure_layout(self, figure: Optional[plt.Figure], n_plots: int, 
                         has_legend: bool, figsize: Tuple[float, float]) -> None:
    if n_plots == 0:
        raise ValueError("没有可显示的子图内容")
    
    # 创建或重用图形对象
    if figure is None:
        self.fig, self.axs = plt.subplots(1, n_plots, figsize=figsize, 
                                          sharey=True, gridspec_kw={'wspace': 0.0})
    else:
        self.fig = figure
        self.fig.clear()
        self.axs = self.fig.subplots(1, n_plots, sharey=True, gridspec_kw={'wspace': 0.0})
    
    # 确保 axs 为列表（单子图时 subplots 返回单个 Axes 对象）
    if n_plots == 1:
        self.axs = [self.axs]
    
    # 根据是否有图例选择底部边距
    bottom_margin = (self.LAYOUT_CONFIG['legend_bottom_margin'] if has_legend 
                     else self.LAYOUT_CONFIG['no_legend_bottom_margin'])
    
    # 调整图形布局参数
    plt.subplots_adjust(
        left=self.LAYOUT_CONFIG['left_margin'],
        right=self.LAYOUT_CONFIG['right_margin'],
        bottom=bottom_margin,
        top=1-self.LAYOUT_CONFIG['title_height'],
        wspace=0.0
    )
```

#### _create_window_size_slider(has_legend)

**目的：** 创建窗口大小滑动条

**滑动条功能：**
- 控制显示窗口的深度范围
- 垂直方向，位于图形右侧
- 范围从最小显示比例到完整深度范围

**代码示例：**
```python
def _create_window_size_slider(self, has_legend: bool) -> None:
    """创建窗口大小滑动条"""
    # 计算滑动条位置和尺寸
    bottom_margin = (self.LAYOUT_CONFIG['legend_bottom_margin'] if has_legend 
                     else self.LAYOUT_CONFIG['no_legend_bottom_margin'])
    slider_height = 1 - self.LAYOUT_CONFIG['title_height'] - bottom_margin
    
    # 创建滑动条轴对象（右侧垂直条）
    slider_ax = plt.axes([
        self.LAYOUT_CONFIG['right_margin'],  # x 位置：右侧边距处
        bottom_margin,  # y 位置：底部边距处
        self.LAYOUT_CONFIG['slider_width'],  # 宽度
        slider_height  # 高度
    ])
    
    # 计算滑动条数值范围
    depth_range = self.depth_max - self.depth_min
    min_window_size = depth_range * self.LAYOUT_CONFIG['min_window_size_ratio']
    max_window_size = depth_range
    initial_window_size = depth_range * 0.5
    
    # 创建滑动条对象
    self.window_size_slider = Slider(
        ax=slider_ax,
        label='',
        valmin=min_window_size,
        valmax=max_window_size,
        valinit=initial_window_size,
        orientation='vertical',
    )
    
    # 隐藏滑动条数值文本，使用自定义文本标签
    self.window_size_slider.valtext.set_visible(False)
    slider_ax.text(0.5, 0.5, '窗口大小 (m)', rotation=270, ha='center', va='center',
                   transform=slider_ax.transAxes, 
                   fontsize=self.LAYOUT_CONFIG['tick_label_fontsize'])
```

#### _create_title_box(ax, title, color, index)

**目的：** 为子图创建标题框（道头）

**标题框设计：**
- 位于子图顶部中央
- 浅灰色背景，细边框
- 粗体文本，指定颜色
- 高 zorder 确保显示在最上层

**代码示例：**
```python
def _create_title_box(self, ax: plt.Axes, title: Any, color: str, index: int) -> None:
    """为子图创建标题框"""
    # 获取子图在图形中的位置（相对坐标）
    orig_pos = ax.get_position()
    
    # 计算标题框位置
    title_bbox = [
        orig_pos.x0 + self.LAYOUT_CONFIG['title_margin'],
        orig_pos.y0 + orig_pos.height + self.LAYOUT_CONFIG['title_margin'],
        orig_pos.width - 2 * self.LAYOUT_CONFIG['title_margin'],
        self.LAYOUT_CONFIG['title_height']
    ]
    
    # 创建标题背景矩形
    title_rect = Rectangle(
        (title_bbox[0], title_bbox[1]), title_bbox[2], title_bbox[3],
        transform=self.fig.transFigure,
        facecolor=self.RENDERING_CONFIG['head_box_facecolor'],
        edgecolor=self.RENDERING_CONFIG['head_box_edgecolor'],
        linewidth=self.RENDERING_CONFIG['title_box_linewidth'],
        clip_on=False,
        zorder=10,
    )
    self.fig.add_artist(title_rect)
    
    # 创建标题文本
    if isinstance(title, list):
        # 多部分标题（多曲线道）
        n_parts = len(title)
        part_height = title_bbox[3] / n_parts
        
        for i, part_text in enumerate(title):
            x_center = title_bbox[0] + title_bbox[2] / 2
            y_center = title_bbox[1] + (i+0.5)*part_height
            
            part_color = self.DEFAULT_MULTI_CURVE_COLORS[i % len(self.DEFAULT_MULTI_CURVE_COLORS)]
            text_obj = Text(
                x_center, y_center, part_text,
                fontsize=self.LAYOUT_CONFIG['title_fontsize'],
                fontweight=self.FONT_CONFIG['weight_bold'],
                color=part_color,
                ha='center', va='center',
                transform=self.fig.transFigure,
                clip_on=False,
                zorder=11
            )
            self.fig.add_artist(text_obj)
    else:
        # 单标题
        title_text = Text(
            title_bbox[0] + title_bbox[2] / 2,
            title_bbox[1] + title_bbox[3] / 2,
            title,
            fontsize=self.LAYOUT_CONFIG['title_fontsize'],
            fontweight=self.FONT_CONFIG['weight_bold'],
            color=color,
            ha='center', va='center',
            transform=self.fig.transFigure,
            clip_on=False,
            zorder=11
        )
        self.fig.add_artist(title_text)
```

#### _plot_fmi_panel(ax, image_data, title, index)

**目的：** 绘制 FMI 图像面板

**支持图像格式：**
- 2D 灰度图像：使用热力图色彩映射
- 3D 彩色图像：直接显示 RGB 或 RGBA
- 3D 单通道：转换为 2D 显示

**代码示例：**
```python
def _plot_fmi_panel(self, ax: plt.Axes, image_data: np.ndarray, 
                    title: str, index: int) -> None:
    """绘制 FMI 图像面板"""
    # 创建标题框
    self._create_title_box(ax, title, '#222222', index)
    
    fmi_depth = self.fmi_data_windows['depth']
    
    # 根据图像维度选择显示方法
    if len(image_data.shape) == 2:
        # 2D 图像：使用热力图色彩映射
        img = ax.imshow(image_data, aspect='auto', 
                       cmap=self.config_fmi['color_map'],
                       extent=[0, image_data.shape[1], fmi_depth[-1], fmi_depth[0]])
    
    elif len(image_data.shape) == 3 and image_data.shape[2] in [1, 3, 4]:
        # 3D 图像：单通道转换为 2D，多通道直接显示
        display_data = image_data if image_data.shape[2] != 1 else image_data[:, :, 0]
        img = ax.imshow(display_data, aspect='auto',
                       extent=[0, image_data.shape[1], fmi_depth[-1], fmi_depth[0]])
    else:
        raise ValueError(f"不支持的图像维度：{image_data.shape}")
    
    # 保存图像对象用于后续更新
    self.fmi_images.append(img)
    
    # 设置坐标轴：隐藏 X 轴标签
    ax.tick_params(axis='x', which='both', bottom=False, top=False, labelbottom=False)
    ax.set_xlabel('')
    ax.set_ylim(fmi_depth[-1], fmi_depth[0])
    
    # Y 轴显示逻辑：只有第一个面板显示 Y 轴标签
    if index == 0:
        ax.set_ylabel('深度 (m)', fontsize=10)
        ax.tick_params(axis='y', labelsize=8)
    else:
        ax.tick_params(left=False, labelleft=False)
```

#### _plot_curve_panel(ax, curve_item, color, index, line_style, line_width)

**目的：** 绘制单个曲线面板

**曲线显示特性：**
- 深度在 Y 轴，曲线值在 X 轴
- 自动计算合适的显示范围
- 添加网格线提高可读性
- 反转 Y 轴符合地质显示习惯（深度向下增加）

**支持模式：**
- 单曲线道：一个面板显示一条曲线
- 多曲线道：一个面板显示多条曲线（curve_item 为列表）

**代码示例：**
```python
def _plot_curve_panel(self, ax: plt.Axes, curve_item: Any, color: str, 
                      index: int, line_style: Any, line_width: Any) -> None:
    """绘制单个曲线面板"""
    if isinstance(curve_item, list):
        # 多曲线道 - 绘制多条曲线
        self._create_title_box(ax, curve_item, color, index)
        
        # 为每条曲线分配颜色
        for i, curve_col in enumerate(curve_item):
            curve_color = color[i]
            width = line_width[i] if isinstance(line_width, list) else line_width
            style = line_style[i] if isinstance(line_style, list) else line_style
            
            line, = ax.plot(
                self.logging_data_windows[curve_col].values,
                self.logging_data_windows[self.config_logging['depth_col']].values,
                color=curve_color, linewidth=width, linestyle=style, label=curve_col
            )
            self.plots.append(line)
        
        # 计算多曲线的联合显示范围
        all_min, all_max = float('inf'), float('-inf')
        for curve_col in curve_item:
            min_val, max_val = self.data_manager._calculate_curve_display_limits(curve_col)
            all_min = min(all_min, min_val)
            all_max = max(all_max, max_val)
        
        ax.set_xlim(all_min, all_max)
        
    elif isinstance(curve_item, str):
        # 单曲线道
        self._create_title_box(ax, curve_item, color, index)
        
        # 绘制曲线：X=曲线值，Y=深度
        line, = ax.plot(
            self.logging_data_windows[curve_item].values,
            self.logging_data_windows[self.config_logging['depth_col']].values,
            color=color, linewidth=line_width, linestyle=line_style, label=curve_item
        )
        self.plots.append(line)
        
        # 设置 X 轴显示范围
        min_val, max_val = self.data_manager._calculate_curve_display_limits(curve_item)
        ax.set_xlim(min_val, max_val)
    else:
        raise ValueError(f"不支持的曲线项类型：{type(curve_item)}")
    
    # 设置坐标轴属性
    ax.grid(True, alpha=0.3)  # 添加半透明网格线
    
    # Y 轴显示逻辑
    if not self.fmi_axes and index == 0:
        ax.set_ylabel('深度 (m)', fontsize=10)
        ax.tick_params(axis='y', labelsize=8)
    else:
        ax.tick_params(left=False, labelleft=False)
```

#### _batch_render_classification(ax, class_col, visible_data)

**目的：** 批量渲染分类数据（性能优化版本）

**优化策略：**
- 使用 PolyCollection 批量绘制多边形，比逐个绘制矩形性能更高
- 按分类值分组，相同颜色的矩形批量处理
- 减少 matplotlib 绘图调用次数

**代码示例：**
```python
def _batch_render_classification(self, ax: plt.Axes, class_col: str, 
                                  visible_data: pd.DataFrame) -> None:
    """批量渲染分类数据（性能优化版本）"""
    if visible_data.empty:
        return
    
    # 按分类值分组数据
    class_groups = visible_data.groupby(class_col)
    vertices_list = []  # 存储所有矩形的顶点
    colors_list = []    # 存储对应的颜色
    
    # 为每个分类值创建矩形
    for class_val, group in class_groups:
        if pd.isna(class_val) or class_val < 0:
            continue  # 跳过无效值
        
        class_int = int(class_val)
        xmax = self.litho_width_config.get(class_int, 0.1)
        color = self.config_type['colors_type'][class_int]
        
        # 为每个深度点创建矩形
        for depth in group[self.config_logging['depth_col']]:
            y_bottom = depth - self.resolution / 2
            y_top = depth + self.resolution / 2
            
            # 定义矩形的四个顶点
            vertices = [[0, y_bottom], [xmax, y_bottom], 
                       [xmax, y_top], [0, y_top]]
            vertices_list.append(vertices)
            colors_list.append(color)
    
    # 如果有矩形数据，批量绘制
    if vertices_list:
        poly_collection = PolyCollection(
            vertices_list,
            facecolors=colors_list,
            edgecolors='none',
            linewidths=0
        )
        ax.add_collection(poly_collection)
```

#### _plot_nmr_panel(ax, nmr_index, panel_index)

**目的：** 绘制单个 NMR 谱面板

**NMR 谱特性：**
- X 轴为 T2 弛豫时间（通常使用对数坐标）
- Y 轴为深度（与测井曲线共享）
- 谱图使用填充折线显示

**代码示例：**
```python
def _plot_nmr_panel(self, ax: plt.Axes, nmr_index: int, panel_index: int) -> None:
    """绘制单个 NMR 谱面板"""
    title = self.config_nmr.get('nmr_title', 
            [f'NMR_{i+1}' for i in range(10)])[nmr_index]
    
    # 创建标题框
    self._create_title_box(ax, title, '#000000', panel_index)
    
    # 设置坐标轴属性
    ax.set_xlabel('')
    ax.set_ylabel('')
    ax.grid(True, alpha=0.3)
    
    # 移除 X 轴的刻度线和刻度标签
    ax.tick_params(axis='x', which='both', bottom=False, top=False, labelbottom=False)
    
    if self.config_nmr['x_logarithmic_scale']:
        ax.set_xscale('log')  # T2 核磁谱设置对数坐标
    
    # 初始化对象池，避免频繁创建销毁
    nmr_plot_info = {
        'ax': ax,
        'nmr_index': nmr_index,
        'line_pool': [],    # 谱线对象池
        'fill_pool': [],    # 填充对象池
        'active_lines': [], # 当前活动的谱线
        'active_fills': [], # 当前活动的填充
        'max_pool_size': 50 # 对象池最大容量
    }
    self.nmr_plots.append(nmr_plot_info)
    
    # 预创建对象池
    self._initialize_nmr_object_pool(nmr_plot_info)
    
    # 隐藏 Y 轴标签（非第一个面板）
    if panel_index > 0:
        ax.tick_params(left=False, labelleft=False)
```

#### _initialize_nmr_object_pool(nmr_plot)

**目的：** 初始化 NMR 绘图对象池

**原理：**
- 预创建谱线和填充对象，避免运行时频繁创建销毁
- 对象池大小根据数据量动态扩展
- 使用可见性控制而非创建/销毁提高性能

**代码示例：**
```python
def _initialize_nmr_object_pool(self, nmr_plot: Dict[str, Any]) -> None:
    """初始化 NMR 绘图对象池"""
    ax = nmr_plot['ax']
    max_pool_size = nmr_plot['max_pool_size']
    
    # 预创建谱线对象
    for i in range(max_pool_size):
        line, = ax.plot([], [], 'g-', 
                       linewidth=self.config_nmr['spectrum_config']['line_width'],
                       alpha=self.config_nmr['spectrum_config']['fill_alpha'],
                       visible=False)
        nmr_plot['line_pool'].append(line)
    
    # 预创建填充对象
    for i in range(max_pool_size):
        fill = ax.fill_between([], [], [],
                              linewidth=self.config_nmr['spectrum_config']['line_width'],
                              alpha=self.config_nmr['spectrum_config']['fill_alpha'],
                              color=self.config_nmr['color_fill'],
                              visible=False)
        nmr_plot['fill_pool'].append(fill)
```

#### _create_legend_panel(legend_dict)

**目的：** 创建图例面板

**图例设计：**
- 位于图形底部中央
- 多列布局，自动调整列数
- 半透明背景，细边框
- 使用岩性对应的颜色显示

**代码示例：**
```python
def _create_legend_panel(self, legend_dict: Dict[int, str]) -> None:
    """创建图例面板"""
    if not legend_dict:
        return
    
    n_items = len(legend_dict)
    legend_height = 0.02
    legend_width = min(0.8, n_items * 0.15)
    
    # 创建图例轴对象（底部中央，无边框）
    legend_ax = plt.axes([0.5 - legend_width / 2, 0.01, legend_width, legend_height], 
                         frameon=False)
    legend_ax.set_axis_off()
    
    # 准备图例句柄和标签
    handles, labels = [], []
    for key in sorted(legend_dict.keys()):
        patch = Rectangle((0, 0), 1, 1,
                         facecolor=self.config_type['colors_type'][int(key)],
                         edgecolor='black',
                         linewidth=self.config_type['display_config']['border_width']
                         )
        handles.append(patch)
        labels.append(legend_dict[key])
    
    # 创建图例
    legend = legend_ax.legend(handles, labels, loc='center',
                             ncol=self.config_type['display_config']['legend_columns'],
                             frameon=True,
                             framealpha=self.config_type['display_config']['pattern_alpha'],
                             fontsize=self.config_type['display_config']['legend_fontsize']
                             )
    
    # 设置图例框样式
    frame = legend.get_frame()
    frame.set_facecolor(self.config_type['display_config']['legend_facecolor'])
```

#### _on_window_size_change(val)

**目的：** 窗口大小变化事件处理

**处理逻辑：**
1. 更新窗口大小
2. 调整深度位置确保不超出有效范围
3. 触发显示更新

**代码示例：**
```python
def _on_window_size_change(self, val: float) -> None:
    """窗口大小变化事件处理"""
    if val <= 0:
        return
    
    self.window_size = val
    
    # 计算最大有效深度位置
    max_valid_position = self.depth_max - self.window_size
    if self.depth_position > max_valid_position:
        self.depth_position = max_valid_position
    
    self._update_display()
```

#### _on_mouse_scroll(event)

**目的：** 鼠标滚轮事件处理：实现深度滚动

**滚动逻辑：**
- 向上滚动：向浅部（小深度）滚动
- 向下滚动：向深部（大深度）滚动
- 步长为窗口大小的 10%

**代码示例：**
```python
def _on_mouse_scroll(self, event) -> None:
    """鼠标滚轮事件处理：实现深度滚动"""
    # 检查事件是否发生在子图内
    if event.inaxes not in self.axs:
        return
    
    # 计算滚动步长（窗口大小的 10%）
    step = self.window_size * self.LAYOUT_CONFIG['scroll_step_ratio']
    
    # 根据滚轮方向计算新位置
    if event.button == 'up':
        new_position = self.depth_position - step  # 向上滚动：向浅部
    elif event.button == 'down':
        new_position = self.depth_position + step  # 向下滚动：向深部
    else:
        return
    
    # 限制位置范围
    new_position = max(self.depth_min, min(new_position, self.depth_max - self.window_size))
    
    if new_position != self.depth_position:
        self.depth_position = new_position
        self._update_display()
```

#### _update_display()

**目的：** 更新显示内容（核心刷新函数）

**刷新流程：**
1. 计算当前显示范围
2. 从数据管理器获取可见范围内的各类数据
3. 更新所有子图的 Y 轴范围
4. 条件更新各显示组件（曲线、分类、FMI、NMR）
5. 更新深度指示器
6. 重绘图形
7. 记录性能数据

**代码示例：**
```python
def _update_display(self) -> None:
    """更新显示内容（核心刷新函数）"""
    start_time = time.time()
    
    # 计算当前显示的深度范围
    top_depth = self.depth_position
    bottom_depth = self.depth_position + self.window_size
    
    # 获取可见范围内的各类数据
    self.logging_data_windows = self.data_manager.get_visible_logging_data(top_depth, bottom_depth)
    self.fmi_data_windows = self.data_manager.get_visible_fmi_data(top_depth, bottom_depth, 
                                                                    self.config_fmi['pix_density_y'])
    nmr_plot_density = self.config_nmr['plot_density'] / self.window_size
    self.nmr_data_windows = self.data_manager.get_visible_nmr_data(top_depth, bottom_depth, 
                                                                    nmr_plot_density)
    
    # 更新所有子图的 Y 轴范围
    for ax in self.axs:
        ax.set_ylim(top_depth, bottom_depth)
    
    # 条件更新各显示组件
    if self.logging_data_windows is not None:
        self._update_curve_panels()
    self.logger.debug("渲染常规测井完成：%.1fms", (time.time() - start_time) * 1000)
    start_time = time.time()
    
    if self.logging_data_windows is not None and self.config_type['types_cols']:
        self._update_classification_panels()
    self.logger.debug("渲染分类结果完成：%.1fms", (time.time() - start_time) * 1000)
    start_time = time.time()
    
    if self.fmi_data_windows is not None:
        self._update_fmi_display()
    self.logger.debug("渲染电成像完成：%.1fms", (time.time() - start_time) * 1000)
    start_time = time.time()
    
    if self.nmr_data_windows is not None:
        self._update_nmr_display()
    self.logger.debug("渲染核磁完成：%.1fms", (time.time() - start_time) * 1000)
    start_time = time.time()
    
    self._update_depth_indicator(top_depth, bottom_depth)
    
    # 确保 y 轴方向正确（深度从上到下递增）
    self._ensure_correct_yaxis_direction()
    
    # 请求图形重绘
    self.fig.canvas.draw_idle()
```

#### _update_curve_panels()

**目的：** 更新常规测井曲线面板的数据

**关键修复：**
- 使用 `line.set_data()` 更新曲线数据而非重新绘制
- 确保曲线数据随深度范围变化而更新

**代码示例：**
```python
def _update_curve_panels(self) -> None:
    """更新常规测井曲线面板的数据"""
    if self.logging_data_windows is None or not self.plots:
        return
    
    depth_col = self.config_logging['depth_col']
    depth_data = self.logging_data_windows[depth_col].values
    
    # 遍历所有曲线绘图对象并更新数据
    plot_index = 0
    curve_configs = self.config_logging['curves_plot']
    
    for i, curve_item in enumerate(curve_configs):
        if plot_index >= len(self.plots):
            break
        
        if isinstance(curve_item, list):
            # 多曲线道：更新该道内的所有曲线
            for curve_col in curve_item:
                if plot_index < len(self.plots) and curve_col in self.logging_data_windows.columns:
                    line = self.plots[plot_index]
                    curve_values = self.logging_data_windows[curve_col].values
                    
                    # 关键修复：更新线对象的数据
                    line.set_data(curve_values, depth_data)
                    plot_index += 1
        elif isinstance(curve_item, str):
            # 单曲线道：更新单条曲线
            if curve_item in self.logging_data_windows.columns:
                line = self.plots[plot_index]
                curve_values = self.logging_data_windows[curve_item].values
                
                line.set_data(curve_values, depth_data)
                plot_index += 1
```

#### _update_classification_panels()

**目的：** 更新分类面板显示

**更新策略：**
1. 清除现有内容
2. 获取可见范围内的数据
3. 使用批量渲染方法重新绘制

**代码示例：**
```python
def _update_classification_panels(self) -> None:
    """更新分类面板显示"""
    if not self.class_axes:
        return
    
    # 更新每个分类面板
    for i, (ax, col) in enumerate(zip(self.class_axes, self.config_type['types_cols'])):
        ax.clear()  # 清除现有内容
        
        # 重新设置坐标轴属性
        ax.set_xticks([])
        ax.tick_params(left=False, labelleft=False)
        ax.set_ylim(self.depth_position, self.depth_position + self.window_size)
        
        # 使用批量渲染方法绘制分类数据
        self._batch_render_classification(ax, col, self.logging_data_windows)
```

#### _update_fmi_display()

**目的：** 更新 FMI 图像显示（使用缓存优化）

**关键修复：**
- 更新图像数据
- 更新 extent 参数以匹配当前深度范围

**代码示例：**
```python
def _update_fmi_display(self) -> None:
    """更新 FMI 图像显示"""
    if not self.fmi_data_windows or not self.fmi_images:
        return
    
    fmi_depth = self.fmi_data_windows['depth']
    
    # 更新每个 FMI 图像
    for i, (img, image_data) in enumerate(zip(self.fmi_images, 
                                               self.fmi_data_windows['image_data'])):
        if len(image_data.shape) == 2:
            img.set_data(image_data)
            # 关键修复：更新 extent 参数
            img.set_extent([0, image_data.shape[1], fmi_depth[-1], fmi_depth[0]])
        elif len(image_data.shape) == 3:
            display_data = image_data if image_data.shape[2] != 1 else image_data[:, :, 0]
            img.set_data(display_data)
            img.set_extent([0, image_data.shape[1], fmi_depth[-1], fmi_depth[0]])
        
        if self.config_fmi['auto_scale']:
            img.set_clim(vmin=np.min(image_data), vmax=np.max(image_data))
```

#### _update_nmr_display()

**目的：** 更新 NMR 谱图显示

**更新策略：**
- 使用对象池复用谱线和填充对象
- 隐藏不需要的对象而非销毁
- 根据数据量动态扩展对象池

**代码示例：**
```python
def _update_nmr_display(self) -> None:
    """更新 NMR 谱图显示"""
    if not self.nmr_plots or not self.nmr_data_windows:
        return
    
    if 'nmr_data' in self.nmr_data_windows and self.nmr_data_windows['nmr_data']:
        nmr_data_list = self.nmr_data_windows['nmr_data']
        nmr_depth = self.nmr_data_windows['depth']
    else:
        return
    
    for i, nmr_plot in enumerate(self.nmr_plots):
        ax = nmr_plot['ax']
        
        # 隐藏所有当前活动的对象
        self._hide_all_active_objects(nmr_plot)
        
        nmr_data = nmr_data_list[i]
        needed_objects = nmr_data.shape[0]
        
        # 确保对象池足够大
        if needed_objects > nmr_plot['max_pool_size']:
            self._expand_object_pool(nmr_plot, needed_objects)
        
        # 计算振幅缩放因子
        scale_factor = self.config_nmr['plot_amplitude_scaling'] * \
                      (nmr_depth.max() - nmr_depth.min()) / 20
        nmr_data = nmr_data * scale_factor
        
        # 为每个深度的 NMR 数据绘制谱图
        for i in range(nmr_data.shape[0]):
            if i >= len(nmr_plot['line_pool']):
                break
            
            NMR_X = np.linspace(0.001, nmr_data.shape[1], nmr_data.shape[1])
            NMR_Y = nmr_data[i, :]
            
            # 归一化振幅并添加深度偏移
            normalized_NMR_Y = NMR_Y
            y_values = nmr_depth[i] - normalized_NMR_Y
            baseline = np.full_like(y_values, nmr_depth[i])
            
            # 复用对象池中的对象
            line = nmr_plot['line_pool'][i]
            line.set_data(NMR_X, y_values)
            line.set_visible(True)
            nmr_plot['active_lines'].append(line)
            
            # 创建新的填充
            new_fill = ax.fill_between(NMR_X, baseline, y_values,
                                       alpha=self.config_nmr['spectrum_config']['fill_alpha'],
                                       color=self.config_nmr['color_fill'],
                                       linewidth=0)
            nmr_plot['fill_pool'][i] = new_fill
            nmr_plot['active_fills'].append(new_fill)
```

#### _update_depth_indicator(top_depth, bottom_depth)

**目的：** 更新深度指示器：显示当前视图的深度范围和窗口大小

**位置：** 图形右下角，半透明背景确保可读性

**代码示例：**
```python
def _update_depth_indicator(self, top_depth: float, bottom_depth: float) -> None:
    """更新深度指示器"""
    indicator_text = (f" 深度[{self.depth_min:.2f}({top_depth:.2f}-{bottom_depth:.2f})"
                      f"{self.depth_max:.2f}] | 窗口：{self.window_size:.2f} m ")
    
    if hasattr(self, '_depth_indicator'):
        # 更新现有文本对象
        self._depth_indicator.set_text(indicator_text)
    else:
        # 创建新文本对象（首次调用时）
        self._depth_indicator = self.fig.text(
            0.99, 0.01, indicator_text,
            ha='right', va='bottom',
            fontsize=self.LAYOUT_CONFIG['depth_indicator_fontsize'],
            bbox=dict(facecolor=self.LAYOUT_CONFIG['depth_indicator_facecolor'],
                     alpha=self.LAYOUT_CONFIG['depth_indicator_alpha'],
                     boxstyle='round,pad=0.1')
        )
```

#### visualize(figsize, figure)

**目的：** 主可视化函数：完整的测井数据可视化流程

**执行流程：**
1. 参数预处理和验证
2. 获取待显示段数据
3. 图形设置和布局
4. 数据绘制和优化
5. 交互功能设置
6. 初始显示和性能监控

**代码示例：**
```python
def visualize(self, figsize: Tuple[float, float] = (16, 10),
              figure: Optional[plt.Figure] = None) -> None:
    """主可视化函数：完整的测井数据可视化流程"""
    try:
        self.logger.info("开始测井数据可视化")
        start_time = time.time()
        
        # ========== 获取待显示段数据 ==========
        self.logging_data_windows = self.data_manager.get_visible_logging_data(
            self.depth_position, self.depth_position+self.window_size)
        self.fmi_data_windows = self.data_manager.get_visible_fmi_data(
            self.depth_position, self.depth_position+self.window_size,
            self.config_fmi['pix_density_y'])
        nmr_plot_density = self.config_nmr['plot_density'] / self.window_size
        self.nmr_data_windows = self.data_manager.get_visible_nmr_data(
            self.depth_position, self.depth_position+self.window_size,
            nmr_plot_density)
        
        # ========== 图形设置 ==========
        n_plots = self._calculate_subplot_count()
        has_legend = bool(self.config_type['show_legend'])
        self._setup_figure_layout(figure, n_plots, has_legend, figsize)
        self._create_window_size_slider(has_legend)
        
        # ========== 优化和绘制 ==========
        self._optimize_fmi_rendering()
        self._plot_all_fmi_panels()
        self._plot_all_curves()
        self._plot_all_classification_panels()
        self._plot_all_nmr_panels()
        
        # ========== 交互功能 ==========
        self.window_size_slider.on_changed(self._on_window_size_change)
        self.fig.canvas.mpl_connect('scroll_event', self._on_mouse_scroll)
        self._create_legend_panel(self.config_type['legend_dict'])
        
        # ========== 初始显示 ==========
        self._update_display()
        
        # ========== 性能统计 ==========
        total_time = time.time() - start_time
        self.logger.info("可视化完成，耗时：%.2fs", total_time)
        plt.show()
        
    except Exception as e:
        self.logger.error("可视化失败：%s", str(e))
        if self.fig:
            plt.close(self.fig)
        raise
```

---

## 四、LoggingDataManager 数据管理器

### 4.1 类结构概览

```python
class LoggingDataManager:
    """测井数据管理器
    
    职责：
    - 数据验证与预处理
    - 绘图配置智能检查与补全
    - 深度窗口数据缓存管理
    - 性能统计与监控
    """
```

### 4.2 性能配置

```python
PERFORMANCE_CONFIG = {
    'cache_enabled': True,         # 启用缓存
    'max_cache_size': 500,         # 常规数据最大缓存数
    'compression_level': 1,        # 压缩级别 1-9
    'fmi_cache_size': 50,          # FMI 缓存大小
    'nmr_cache_size': 100,         # NMR 缓存大小
}
```

### 4.3 核心属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `depth_min` | float | 数据最小深度 |
| `depth_max` | float | 数据最大深度 |
| `cache_system` | EnhancedWellLogCache | 缓存系统实例 |
| `resolution` | float | 深度采样分辨率 |
| `logging_data` | pd.DataFrame | 常规测井数据 |
| `fmi_data` | Dict | FMI 电成像数据 |
| `nmr_data` | Dict | NMR 核磁数据 |
| `config_logging` | Dict | 测井数据绘图配置 |
| `config_fmi` | Dict | FMI 数据绘图配置 |
| `config_nmr` | Dict | NMR 数据绘图配置 |
| `config_type` | Dict | 分类数据绘图配置 |

### 4.4 核心方法详解

#### _validate_logging_data(logging_data)

**目的：** 验证常规测井数据的有效性

**验证流程：**
1. 检查数据类型是否为 DataFrame
2. 检查深度列是否严格递增
3. 检查深度间隔是否合理
4. 计算深度分辨率

**代码示例：**
```python
def _validate_logging_data(self, logging_data: pd.DataFrame=None) -> None:
    """验证输入参数的有效性"""
    if logging_data is None or logging_data.empty:
        logger.info("无 logging 数据")
        return None
    
    if not isinstance(logging_data, pd.DataFrame):
        raise ValueError("logging_data 参数必须是 pandas DataFrame 类型")
    
    # 检查深度列
    if not logging_data.columns[0].upper().__contains__("DEPTH"):
        logger.warning('logging_data 第一列不为深度列，列名为{}'.format(
            logging_data.columns[0]))
    
    # 检查深度间隔
    depth_diff = logging_data[logging_data.columns[0]].diff().dropna()
    if len(depth_diff) > 0:
        avg_interval = depth_diff.mean()
        std_interval = depth_diff.std()
        logger.info(f"深度间隔统计：平均={avg_interval:.4f}, 标准差={std_interval:.4f}")
        
        # 检查异常间隔
        unusual_intervals = depth_diff[(depth_diff > avg_interval + 3*std_interval)]
        mistake_intervals = depth_diff[(depth_diff <= 0)]
        if len(unusual_intervals) > 0 or len(mistake_intervals) > 0:
            logger.warning(f"发现 {len(unusual_intervals)} 个异常深度间隔，"
                          f"{len(mistake_intervals)} 个错误深度间隔")
    
    # 计算分辨率
    self.resolution = get_resolution_by_depth(
        logging_data[logging_data.columns[0]].dropna().values)
```

#### _validate_fmi_data(fmi_dict)

**目的：** 验证 FMI 图像数据的结构和完整性

**验证内容：**
- 必要键是否存在（depth, image_data）
- 深度数据是否为一维数组
- 图像数据维度是否正确（2D 或 3D）
- 深度维度是否匹配

**代码示例：**
```python
def _validate_fmi_data(self, fmi_dict: Optional[Dict[str, Any]]) -> None:
    """验证 FMI 图像数据的结构和完整性"""
    if fmi_dict is None:
        logger.info("无 FMI 数据")
        return
    
    # 检查必要键
    required_keys = ['depth', 'image_data']
    for key in required_keys:
        if key not in fmi_dict:
            raise ValueError(f"FMI 字典缺少必要键：{key}")
    
    # 验证深度数据
    depth_data = fmi_dict['depth']
    if depth_data is None:
        raise ValueError("FMI 深度数据不能为 None")
    
    if depth_data.ndim != 1:
        logger.warning(f"FMI 深度数据必须是一维数组，已自动转换")
        fmi_dict["depth"] = depth_data.ravel()
    
    # 验证图像数据
    if not isinstance(fmi_dict['image_data'], list) or len(fmi_dict['image_data']) == 0:
        raise ValueError("FMI 图像数据必须是非空列表")
    
    for i, image_data in enumerate(fmi_dict['image_data']):
        if not isinstance(image_data, np.ndarray):
            raise ValueError(f"FMI 图像数据 [{i}] 必须是 numpy 数组")
        
        if image_data.ndim not in [2, 3]:
            raise ValueError(f"FMI 图像数据 [{i}] 必须是 2D 或 3D 数组")
        
        if image_data.shape[0] != len(depth_data):
            logger.warning(f"FMI 图像数据 [{i}] 深度维度不匹配")
    
    # 自动生成标题
    if 'title' not in fmi_dict or fmi_dict['title'] is None:
        fmi_dict['title'] = [f'FMI_{i + 1}' for i in range(len(fmi_dict['image_data']))]
```

#### _validate_nmr_data(nmr_dict)

**目的：** 验证 NMR 谱数据的结构

**验证内容：**
- 必要键是否存在（depth, nmr_data）
- nmr_data 是否为列表
- 每个 NMR 数据道是否为 2D 数组
- 深度维度是否匹配

**代码示例：**
```python
def _validate_nmr_data(self, nmr_dict: Optional[Dict[str, Any]]=None) -> None:
    """验证 NMR 谱数据"""
    if nmr_dict is None:
        logger.info("无 NMR 数据")
        return
    
    if not isinstance(nmr_dict, dict):
        raise ValueError("NMR_dict 必须是字典类型")
    
    # 检查必要键
    required_keys = ['depth', 'nmr_data']
    for key in required_keys:
        if key not in nmr_dict:
            raise ValueError(f"NMR 字典缺少必要键：{key}")
    
    # 验证深度数据
    depth_data = nmr_dict['depth']
    if depth_data is None:
        raise ValueError("NMR 深度数据不能为 None")
    
    if depth_data.ndim != 1:
        logger.warning("NMR 深度数据必须是一维数组，已自动转换")
        nmr_dict["depth"] = depth_data.ravel()
    
    # 验证 nmr_data
    if not isinstance(nmr_dict['nmr_data'], list) or len(nmr_dict['nmr_data']) == 0:
        raise ValueError("NMR 数据必须是非空列表")
    
    for i, nmr_array in enumerate(nmr_dict['nmr_data']):
        if not isinstance(nmr_array, np.ndarray):
            raise ValueError(f"NMR 数据 [{i}] 必须是 numpy 数组")
        if nmr_array.ndim != 2:
            raise ValueError(f"NMR 数据 [{i}] 必须是 2D 数组")
        if nmr_array.shape[0] != len(depth_data):
            logger.warning(f"NMR 数据 [{i}] 深度维度不匹配")
    
    # 自动生成标题
    if 'title' not in nmr_dict or nmr_dict['title'] is None:
        nmr_dict['title'] = [f'NMR_{i + 1}' for i in range(len(nmr_dict['nmr_data']))]
```

#### _get_depth_limits()

**目的：** 计算深度显示范围

**处理逻辑：**
1. 检查各类数据的深度范围
2. 取所有数据的最小/最大深度
3. 如果没有数据，使用默认范围

**代码示例：**
```python
def _get_depth_limits(self) -> [float, float]:
    """设置深度显示范围"""
    full_depth_min = float('inf')
    full_depth_max = float('-inf')
    
    # 检查常规测井数据
    if self.logging_data is not None and not self.logging_data.empty:
        full_depth_min = min(full_depth_min, 
                            self.logging_data[self.logging_data.columns[0]].min())
        full_depth_max = max(full_depth_max,
                            self.logging_data[self.logging_data.columns[0]].max())
    
    # 检查 FMI 数据
    if self.fmi_data is not None and 'depth' in self.fmi_data:
        full_depth_min = min(full_depth_min, self.fmi_data['depth'].min())
        full_depth_max = max(full_depth_max, self.fmi_data['depth'].max())
    
    # 检查 NMR 数据
    if self.nmr_data is not None:
        full_depth_min = min(full_depth_min, self.nmr_data['depth'].min())
        full_depth_max = max(full_depth_max, self.nmr_data['depth'].max())
    
    # 如果没有数据，使用默认范围
    if full_depth_min == float('inf'):
        full_depth_min = 0.0
        full_depth_max = 100.0
        logger.warning("未找到有效数据，使用默认深度范围：0-100m")
    
    self.depth_min = full_depth_min
    self.depth_max = full_depth_max
    
    return self.depth_min, self.depth_max
```

#### get_visible_logging_data(top_depth, bottom_depth)

**目的：** 获取可见范围内的常规测井数据（带缓存）

**处理流程：**
1. 尝试从缓存获取
2. 缓存未命中则从原始数据查询
3. 设置缓存

**代码示例：**
```python
def get_visible_logging_data(self, top_depth: float, bottom_depth: float
                             ) -> Optional[pd.DataFrame]:
    """获取可见范围内的常规测井数据（带缓存）"""
    if self.logging_data is None or self.logging_data.empty:
        return None
    
    depth_range = (top_depth, bottom_depth)
    
    # 尝试从缓存获取
    cached_data = self.cache_system.get_logging_data(depth_range)
    if cached_data is not None:
        logger.debug(f"常规测井缓存命中：{depth_range}")
        return cached_data
    
    # 缓存未命中，从原始数据查询
    logger.debug(f"常规测井缓存未命中，查询数据：{depth_range}")
    depth_col = self.logging_data.columns[0]
    mask = (self.logging_data[depth_col] >= top_depth) & \
           (self.logging_data[depth_col] <= bottom_depth)
    visible_data = self.logging_data[mask].copy()
    
    if not visible_data.empty:
        # 设置缓存
        self.cache_system.set_logging_data(depth_range, visible_data)
        logger.debug(f"常规测井缓存设置：{depth_range}, 数据点{len(visible_data)}")
    
    return visible_data
```

#### get_visible_fmi_data(top_depth, bottom_depth, max_vertical_points)

**目的：** 获取可见范围内的 FMI 数据（带缓存和压缩）

**处理流程：**
1. 尝试从缓存获取
2. 缓存未命中则提取深度切片
3. 垂直压缩图像数据
4. 设置缓存

**代码示例：**
```python
def get_visible_fmi_data(self, top_depth: float, bottom_depth: float,
                         max_vertical_points: int=400) -> Optional[Dict[str, Any]]:
    """获取可见范围内的 FMI 数据（带缓存）"""
    if self.fmi_data is None:
        return None
    
    depth_range = (top_depth, bottom_depth)
    
    # 尝试从缓存获取
    cached_fmi = self.cache_system.get_fmi_data(depth_range)
    if cached_fmi is not None:
        logger.debug(f"FMI 缓存命中：{depth_range}")
        depth_temp = self._get_fmi_depth_slice(top_depth, bottom_depth)
        max_point = cached_fmi[0].shape[0]
        return {
            'depth': self._compress_depths(depth_temp, max_point),
            'image_data': cached_fmi,
            'title': self.fmi_data.get('title',
                    [f'FMI_{i + 1}' for i in range(len(cached_fmi))])
        }
    
    # 缓存未命中，处理 FMI 数据
    logger.debug(f"FMI 缓存未命中，处理数据：{depth_range}")
    depth_mask = (self.fmi_data['depth'] >= top_depth) & \
                 (self.fmi_data['depth'] <= bottom_depth)
    visible_depths = self.fmi_data['depth'][depth_mask]
    
    if len(visible_depths) == 0:
        return None
    
    # 提取并压缩图像数据
    visible_images = []
    max_point = 0
    for image_data in self.fmi_data['image_data']:
        if image_data.shape[0] == len(self.fmi_data['depth']):
            visible_image = image_data[depth_mask]
            compressed_image = self._compress_fmi_vertically(
                visible_image, max_vertical_points)
            max_point = compressed_image.shape[0]
            visible_images.append(compressed_image)
    
    if visible_images:
        # 设置缓存
        self.cache_system.set_fmi_data(depth_range, visible_images)
        
        return {
            'depth': self._compress_depths(visible_depths, max_point),
            'image_data': visible_images,
            'title': self.fmi_data['title']
        }
    
    return None
```

#### _compress_fmi_vertically(image_data, target_points)

**目的：** 对 FMI 图像数据进行垂直方向压缩

**原理：**
- 使用 scipy 的 ndimage.zoom 进行高质量缩放
- 双线性插值保持图像质量
- 仅压缩深度方向，保持宽度不变

**代码示例：**
```python
def _compress_fmi_vertically(self, image_data: np.ndarray, 
                             target_points: int) -> np.ndarray:
    """对 FMI 图像数据进行垂直方向压缩"""
    if image_data.ndim not in [2, 3]:
        raise ValueError(f"不支持的图像维度：{image_data.ndim}")
    
    original_points = image_data.shape[0]
    
    if original_points <= target_points:
        return image_data
    
    # 计算压缩比例
    zoom_factors = [target_points / original_points]
    
    if image_data.ndim == 2:
        zoom_factors.append(1.0)  # 宽度方向不压缩
    else:
        zoom_factors.extend([1.0, 1.0])  # 高度和宽度方向不压缩
    
    # 使用双线性插值进行高质量缩放
    compressed = ndimage.zoom(image_data, zoom_factors, order=1)
    return compressed
```

#### plot_config_check(config_logging, config_fmi, config_nmr, config_type)

**目的：** 绘图配置智能检查与自动补全系统

**功能概述：**
1. 配置验证：检查必填字段和格式正确性
2. 智能补全：缺失配置自动填充合理默认值
3. 数据适配：根据实际数据调整配置参数
4. 一致性检查：确保配置与数据匹配

**返回：**
```python
{
    'logging': 完整的常规测井配置，
    'fmi': 完整的 FMI 配置，
    'nmr': 完整的 NMR 配置，
    'type': 完整的分类配置
}
```

**代码示例：**
```python
def plot_config_check(self, config_logging: Dict[str, Any] = None,
                      config_fmi: Dict[str, Any] = None,
                      config_nmr: Dict[str, Any] = None,
                      config_type: Dict[str, Any] = None
                      ) -> Dict[str, Dict[str, Any]]:
    """绘图配置智能检查与自动补全系统"""
    # 初始化默认配置模板
    default_configs = self._get_default_plot_configs()
    
    # 深度合并用户配置和默认配置
    config_logging = self._deep_merge_configs(default_configs['logging'],
                                               config_logging or {})
    config_fmi = self._deep_merge_configs(default_configs['fmi'], config_fmi or {})
    config_nmr = self._deep_merge_configs(default_configs['nmr'], config_nmr or {})
    config_type = self._deep_merge_configs(default_configs['type'], config_type or {})
    
    # 执行配置验证和适配
    validated_configs = {
        'logging': self._validate_and_adapt_logging_config(config_logging),
        'fmi': self._validate_and_adapt_fmi_config(config_fmi),
        'nmr': self._validate_and_adapt_nmr_config(config_nmr),
        'type': self._validate_and_adapt_type_config(config_type)
    }
    
    # 记录配置检查结果
    self._log_config_check_results(validated_configs)
    
    return validated_configs
```

#### cal_plot_num()

**目的：** 计算各类型面板数量

**返回：**
- `n_curve_panels`: 曲线面板数
- `n_type_panels`: 分类面板数
- `n_fmi_panels`: FMI 面板数
- `n_nmr_panels`: NMR 面板数

**代码示例：**
```python
def cal_plot_num(self):
    """计算子图总数：FMI 面板 + 曲线面板 + 分类面板 + NMR 谱面板"""
    # 曲线面板数
    if self.config_logging and self.config_logging.get('curves_plot'):
        self.n_curve_panels = len(self.config_logging['curves_plot'])
    else:
        self.n_curve_panels = 0
    
    # 分类面板数
    if self.config_type and self.config_type.get('types_cols'):
        self.n_type_panels = len(self.config_type['types_cols'])
    else:
        self.n_type_panels = 0
    
    # FMI 面板数
    if self.fmi_data and self.fmi_data.get('image_data'):
        self.n_fmi_panels = len(self.fmi_data['image_data'])
    else:
        self.n_fmi_panels = 0
    
    # NMR 面板数
    if self.nmr_data and self.nmr_data.get('nmr_data'):
        self.n_nmr_panels = len(self.nmr_data['nmr_data'])
    else:
        self.n_nmr_panels = 0
    
    return self.n_curve_panels, self.n_type_panels, self.n_fmi_panels, self.n_nmr_panels
```

#### _calculate_curve_display_limits(curve_col)

**目的：** 计算曲线的显示范围（X 轴范围）

**范围计算策略：**
1. 过滤异常值（-999 到 999 之外的值）
2. 处理常数数据：添加相对边距
3. 处理非常数数据：添加 5% 边距
4. 确保非负数据的显示范围从 0 或正值开始

**代码示例：**
```python
def _calculate_curve_display_limits(self, curve_col: str):
    """计算曲线的显示范围（X 轴范围）"""
    curve_data = self.logging_data[curve_col]
    
    # 过滤异常值
    valid_mask = (curve_data > -99999) & (curve_data < 999999) & ~curve_data.isna()
    valid_data = curve_data[valid_mask]
    
    if valid_data.empty:
        min_val, max_val = curve_data.min(), curve_data.max()
    else:
        min_val, max_val = valid_data.min(), valid_data.max()
    
    # 处理常数数据
    if abs(max_val - min_val) < 1e-10:
        margin = abs(min_val) * 0.1 if min_val != 0 else 1.0
        min_val -= margin
        max_val += margin
    else:
        # 添加 5% 边距
        data_range = max_val - min_val
        margin = data_range * 0.05
        min_val = max(0, min_val - margin) if min_val >= 0 else min_val - margin
        max_val += margin
    
    return min_val, max_val
```

---

## 五、EnhancedWellLogCache 缓存系统

### 5.1 类结构概览

```python
class EnhancedWellLogCache:
    """增强的测井数据缓存系统
    
    功能：
    - 深度窗口数据缓存
    - 数据压缩存储（zlib）
    - LRU 缓存淘汰策略
    - 性能统计监控
    """
```

### 5.2 缓存配置

```python
@dataclass
class CacheConfig:
    """缓存配置类"""
    enabled: bool = True           # 是否启用缓存
    max_size: int = 100            # 常规数据最大缓存数
    fmi_max_size: int = 50         # FMI 缓存大小（数据量大，缓存较小）
    nmr_max_size: int = 100        # NMR 缓存大小
    compression_level: int = 1     # 压缩级别 1-9（1 最快，9 压缩率最高）
```

### 5.3 核心属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `config` | CacheConfig | 缓存配置对象 |
| `_data_cache` | OrderedDict | 常规数据缓存（DataFrame） |
| `_fmi_cache` | OrderedDict | FMI 图像缓存（压缩存储） |
| `_nmr_cache` | OrderedDict | NMR 数据缓存（压缩存储） |
| `stats` | Dict | 缓存统计（命中率等） |
| `_fmi_compression_stats` | Dict | FMI 压缩统计 |
| `_nmr_compression_stats` | Dict | NMR 压缩统计 |

### 5.4 核心方法详解

#### _generate_cache_key(depth_range, data_type)

**目的：** 生成精确的缓存键

**原理：**
- 使用深度范围作为缓存键
- 高精度（4 位小数）避免冲突
- 区分数据类型前缀

**代码示例：**
```python
def _generate_cache_key(self, depth_range: Tuple[float, float],
                        data_type: str = 'data') -> str:
    """生成精确的缓存键"""
    if data_type == 'fmi':
        return f"fmi_{depth_range[0]:.4f}_{depth_range[1]:.4f}"
    elif data_type == 'nmr':
        return f"nmr_{depth_range[0]:.4f}_{depth_range[1]:.4f}"
    else:
        return f"data_{depth_range[0]:.4f}_{depth_range[1]:.4f}"
```

#### _compress_data(data)

**目的：** 通用数据压缩方法

**原理：**
- 使用 pickle 序列化数据
- 使用 zlib 压缩
- 返回压缩后数据及原始/压缩大小

**代码示例：**
```python
def _compress_data(self, data: Any) -> Tuple[bytes, int, int]:
    """通用数据压缩方法"""
    original_data = pickle.dumps(data)
    original_size = len(original_data)
    compressed = zlib.compress(original_data, self.config.compression_level)
    compressed_size = len(compressed)
    return compressed, original_size, compressed_size
```

#### get_logging_data(depth_range) / set_logging_data(depth_range, data)

**目的：** 获取/设置常规测井数据缓存

**代码示例：**
```python
def get_logging_data(self, depth_range: Tuple[float, float]
                     ) -> Optional[pd.DataFrame]:
    """获取常规测井数据缓存"""
    if not self.config.enabled:
        return None
    
    key = self._generate_cache_key(depth_range, 'data')
    
    if key in self._data_cache:
        self._data_cache.move_to_end(key)  # LRU：移到末尾
        self.stats['data_hits'] += 1
        return self._data_cache[key]
    
    self.stats['data_misses'] += 1
    return None

def set_logging_data(self, depth_range: Tuple[float, float],
                     data: pd.DataFrame):
    """设置常规测井数据缓存"""
    if not self.config.enabled or data is None or data.empty:
        return
    
    key = self._generate_cache_key(depth_range, 'data')
    self._data_cache[key] = data
    self._data_cache.move_to_end(key)
    
    # 维护缓存大小（LRU 淘汰）
    while len(self._data_cache) > self.config.max_size:
        self._data_cache.popitem(last=False)  # 移除最旧的
```

#### get_fmi_data(depth_range) / set_fmi_data(depth_range, fmi_data)

**目的：** 获取/设置 FMI 数据缓存（压缩存储）

**代码示例：**
```python
def get_fmi_data(self, depth_range: Tuple[float, float]
                 ) -> Optional[List[np.ndarray]]:
    """获取 FMI 数据缓存"""
    if not self.config.enabled:
        return None
    
    key = self._generate_cache_key(depth_range, 'fmi')
    
    if key in self._fmi_cache:
        self._fmi_cache.move_to_end(key)
        self.stats['fmi_hits'] += 1
        compressed_data = self._fmi_cache[key]
        fmi_list = self._decompress_data(compressed_data)
        
        # 验证数据格式
        if not isinstance(fmi_list, list) or \
           not all(isinstance(arr, np.ndarray) for arr in fmi_list):
            logger.warning("FMI 缓存数据格式异常，已清除")
            del self._fmi_cache[key]
            return None
        
        return fmi_list
    
    self.stats['fmi_misses'] += 1
    return None

def set_fmi_data(self, depth_range: Tuple[float, float],
                 fmi_data: List[np.ndarray]):
    """设置 FMI 数据缓存"""
    if not self.config.enabled or not fmi_data:
        return
    
    # 验证输入数据格式
    if not isinstance(fmi_data, list):
        logger.warning("FMI 数据必须是列表格式")
        return
    
    valid_arrays = [arr for arr in fmi_data if isinstance(arr, np.ndarray)]
    if not valid_arrays:
        logger.warning("FMI 数据列表中未找到有效的 numpy 数组")
        return
    
    key = self._generate_cache_key(depth_range, 'fmi')
    
    try:
        # 压缩整个列表
        compressed_data, original_size, compressed_size = \
            self._compress_data(valid_arrays)
        
        # 更新压缩统计
        self._fmi_compression_stats['compressed_size'] += compressed_size
        self._fmi_compression_stats['original_size'] += original_size
        
        compression_ratio = original_size / compressed_size if compressed_size > 0 else 1
        logger.debug(f"FMI 数据压缩：{original_size} -> {compressed_size} "
                    f"(比率：{compression_ratio:.2f})")
        
        self._fmi_cache[key] = compressed_data
        self._fmi_cache.move_to_end(key)
        
        # 维护缓存大小
        while len(self._fmi_cache) > self.config.fmi_max_size:
            removed_key, removed_data = self._fmi_cache.popitem(last=False)
            removed_size = len(removed_data)
            self._fmi_compression_stats['compressed_size'] -= removed_size
        
    except Exception as e:
        logger.error(f"FMI 数据压缩失败：{e}")
```

#### get_cache_stats()

**目的：** 获取详细的缓存统计信息

**返回：**
- 缓存大小
- 命中率
- 压缩比率
- 内存节省

**代码示例：**
```python
def get_cache_stats(self) -> Dict[str, Any]:
    """获取详细的缓存统计信息"""
    data_hits, data_misses = self.stats['data_hits'], self.stats['data_misses']
    fmi_hits, fmi_misses = self.stats['fmi_hits'], self.stats['fmi_misses']
    nmr_hits, nmr_misses = self.stats['nmr_hits'], self.stats['nmr_misses']
    
    # 计算命中率
    data_hit_rate = (data_hits / (data_hits + data_misses) * 100
                     if (data_hits + data_misses) > 0 else 0)
    fmi_hit_rate = (fmi_hits / (fmi_hits + fmi_misses) * 100
                    if (fmi_hits + fmi_misses) > 0 else 0)
    nmr_hit_rate = (nmr_hits / (nmr_hits + nmr_misses) * 100
                    if (nmr_hits + nmr_misses) > 0 else 0)
    
    # 计算压缩比率
    fmi_compression_ratio = (self._fmi_compression_stats['original_size'] /
                             self._fmi_compression_stats['compressed_size']
                             if self._fmi_compression_stats['compressed_size'] > 0 else 0)
    nmr_compression_ratio = (self._nmr_compression_stats['original_size'] /
                             self._nmr_compression_stats['compressed_size']
                             if self._nmr_compression_stats['compressed_size'] > 0 else 0)
    
    # 计算内存节省 (MB)
    fmi_memory_saved = (self._fmi_compression_stats['original_size'] -
                        self._fmi_compression_stats['compressed_size']) / (1024 * 1024)
    nmr_memory_saved = (self._nmr_compression_stats['original_size'] -
                        self._nmr_compression_stats['compressed_size']) / (1024 * 1024)
    
    return {
        # 缓存大小
        'data_cache_size': len(self._data_cache),
        'fmi_cache_size': len(self._fmi_cache),
        'nmr_cache_size': len(self._nmr_cache),
        
        # 命中率
        'hit_rates': {
            'data': round(data_hit_rate, 2),
            'fmi': round(fmi_hit_rate, 2),
            'nmr': round(nmr_hit_rate, 2)
        },
        
        # 压缩比率
        'compression_ratios': {
            'fmi': round(fmi_compression_ratio, 2),
            'nmr': round(nmr_compression_ratio, 2)
        },
        
        # 内存节省
        'memory_saved_mb': {
            'fmi': round(fmi_memory_saved, 2),
            'nmr': round(nmr_memory_saved, 2),
            'total': round(fmi_memory_saved + nmr_memory_saved, 2)
        }
    }
```

---

## 六、使用示例

### 6.1 基础使用流程

```python
from src_well_data.data_logging_well import DATA_WELL
from src_plot.data_manager import LoggingDataManager
from src_plot.data_visulization import WellLogVisualizer

# ========== 步骤 1: 加载单井数据 ==========
well = DATA_WELL(
    path_folder=r'F:\logging_workspace\FY1-15',
    WELL_NAME='FY1-15'
)

# 获取文件列表
path_list_logging = well.get_path_list_logging()
path_list_fmi = well.get_path_list_fmi()
path_list_table = well.get_path_list_table()

# ========== 步骤 2: 准备数据 ==========
# 合并测井与表格数据
logging_data = well.combine_logging_table(
    logging_key=path_list_logging[0],
    table_key=path_list_table[0],
    curve_names_logging=['DEPTH', 'GR', 'AC', 'DEN', 'CNL']
)

# 获取 FMI 数据
data_fmi, depth_fmi = well.get_FMI(key=path_list_fmi[0])

# 获取 NMR 数据
data_nmr, depth_nmr = well.get_NMR(key=well.get_path_list_nmr()[0])

# ========== 步骤 3: 创建数据管理器 ==========
LDM = LoggingDataManager(
    logging_data=logging_data,
    fmi_data={'depth': depth_fmi, 'image_data': [data_fmi]},
    nmr_data={'depth': depth_nmr, 'nmr_data': [data_nmr]}
)

# ========== 步骤 4: 配置绘图参数 ==========
config_logging = {
    'depth_col': 'DEPTH',
    'curves_plot': ['GR', 'AC', 'DEN', ['CNL', 'RT']],  # 多曲线道示例
    'colors_plot': ['#FF0000', '#006400', '#00008B', ['#00CED1', '#8B008B']],
    'line_style_plot': ['-', '-', '-', ['-', '-.']],
    'line_width': [1.0, 1.0, 1.0, [1.5, 0.8]]
}

config_fmi = {
    'color_map': 'rainbow',
    'auto_scale': True,
    'scale_range': [0, 256],
    'title_fmi': ['FMI 动态', 'FMI 静态']
}

config_nmr = {
    'plot_density': 40,
    'plot_amplitude_scaling': 0.2,
    'x_logarithmic_scale': True,
    'color_fill': 'green',
    'nmr_title': ['NMR_T2 谱']
}

config_type = {
    'types_cols': ['Lithology'],
    'show_legend': True,
    'colors_type': {0: '#FF6B6B', 1: '#4ECDC4', 2: '#45B7D1'},
    'width_type': {0: 0.2, 1: 0.4, 2: 0.6},
    'legend_dict': {0: '砂岩', 1: '泥岩', 2: '灰岩'},
    'display_config': {
        'legend_columns': 4,
        'legend_facecolor': 'white',
        'border_width': 0.5,
        'legend_fontsize': 10
    }
}

# 验证配置
LDM.plot_config_check(
    config_logging=config_logging,
    config_fmi=config_fmi,
    config_nmr=config_nmr,
    config_type=config_type
)

# ========== 步骤 5: 创建可视化器并显示 ==========
well_viewer = WellLogVisualizer(
    data_manager=LDM,
    config_logging=config_logging,
    config_fmi=config_fmi,
    config_nmr=config_nmr,
    config_type=config_type
)

# 执行可视化
well_viewer.visualize(figsize=(16, 10))
```

### 6.2 自动配置模式

```python
# 使用自动配置（推荐快速预览）
LDM = LoggingDataManager(
    logging_data=logging_data,
    fmi_data={'depth': depth_fmi, 'image_data': [data_fmi]},
    nmr_data={'depth': depth_nmr, 'nmr_data': [data_nmr]}
)

# 自动检测并生成配置
validated_configs = LDM.plot_config_check()

well_viewer = WellLogVisualizer(data_manager=LDM)
well_viewer.visualize()
```

### 6.3 性能监控

```python
# 获取缓存统计
stats = LDM.get_performance_stats()
print(f"常规数据缓存大小：{stats['data_cache_size']}")
print(f"FMI 缓存命中率：{stats['hit_rates']['fmi']}%")
print(f"NMR 压缩比率：{stats['compression_ratios']['nmr']:.2f}x")
print(f"总内存节省：{stats['memory_saved_mb']['total']:.2f} MB")

# 清空缓存
LDM.clear_cache()
```

---

## 七、性能优化

### 7.1 缓存优化

**策略：**
- 深度窗口数据缓存，避免重复查询
- FMI/NMR 数据压缩存储，减少内存占用
- LRU 淘汰策略，保持缓存新鲜度

**效果：**
- 滚动深度时缓存命中率可达 80%+
- FMI 数据压缩比 3-5 倍
- NMR 数据压缩比 4-6 倍

### 7.2 渲染优化

**策略：**
- FMI 图像预压缩（uint8）
- NMR 对象池复用
- 分类数据批量渲染（PolyCollection）
- 曲线数据更新而非重绘（set_data）

**效果：**
- FMI 渲染速度提升 50%
- NMR 谱图内存减少 70%
- 分类数据渲染速度提升 10 倍

### 7.3 交互优化

**策略：**
- 滑动条控制窗口大小
- 鼠标滚轮深度滚动
- 深度指示器实时显示
- 异步重绘（draw_idle）

---

## 八、常见问题

### Q1: 中文显示为方块？

**解决方案：**
```python
# 确保字体配置正确
plt.rcParams['font.family'] = ['DejaVu Sans', 'SimHei', 'sans-serif']
plt.rcParams['axes.unicode_minus'] = False
```

### Q2: 深度轴方向反了？

**解决方案：**
```python
# 确保 Y 轴反转（深度从上到下递增）
ax.invert_yaxis()
# 或
ax.set_ylim(bottom_depth, top_depth)
```

### Q3: 滚动时卡顿？

**解决方案：**
1. 启用缓存
2. 降低 FMI 像素密度（pix_density_y）
3. 降低 NMR 绘制密度（plot_density）
4. 减少同时显示的数据类型

### Q4: 曲线显示范围不合适？

**解决方案：**
```python
# 手动设置 X 轴范围
config_logging['axis_config'] = [
    {'curve': 'GR', 'xlim': [0, 150]},
    {'curve': 'DEN', 'xlim': [1.5, 3.0]}
]
```

---

## 九、扩展方向

### 9.1 可能的功能扩展

1. **多井对比模式：** 并排显示多口井的数据
2. **地层对比线：** 在深度轴上标记地层界面
3. **测量点标记：** 在图上标记取心、测试等位置
4. **导出功能：** 导出为 PDF/PNG/SVG 格式
5. **3D 可视化：** FMI 图像的 3D 井壁展开

### 9.2 与其他模块的协同

- **与数据管理模块协同：** 从 DATA_WELL 获取数据
- **与项目管理模块协同：** 批量可视化多井数据
- **与处理模块协同：** 实时应用校正、滤波等处理

---

*最后更新：2026-03-14*  
*Well Logging Visualization System - Visualization Module v1.0*
